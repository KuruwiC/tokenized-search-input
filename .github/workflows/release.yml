name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type check
        run: pnpm typecheck

      - name: Test
        run: pnpm test:run

      - name: Audit production dependencies
        run: pnpm audit --prod

      - name: Verify release tag
        run: node packages/core/scripts/verify-tag.mjs

      - name: Build, pack, and validate artifact
        run: |
          mkdir -p dist-packages
          cd packages/core && pnpm pack --pack-destination ../../dist-packages
          cd ../..
          node packages/core/scripts/verify-pack.mjs
          pnpm exec publint dist-packages/*.tgz --strict

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist-packages/*.tgz
          generate_release_notes: true
          draft: false
          prerelease: false
